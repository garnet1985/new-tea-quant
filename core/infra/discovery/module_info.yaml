# 模块元数据
name: infra.discovery
version: "0.4.0"
compatible_core_versions: ">=0.4.0"
description: "扩展发现基础设施：门面 Discovery（file / discover / class_discovery）；契约在 contracts。"
dependencies: []
changelog:
  - version: "0.4.0"
    changes:
      - 文档对齐 module-doc-standard：根 API.md / QUICKSTART / TEST_CASES.md；contracts.py
      - 删除 api.yaml、docs/API.md、docs/DECISIONS.md（决策并入 DESIGN）；公开 API 状态最高 beta
  - version: "0.3.0"
    changes:
      - 新增 FileUtils（单个文件操作）
      - 新增 FileDiscovery（批量文件发现）
      - 完善架构：Utils（原子）→ Discovery（批量）；门面 namespace API
  - version: "0.2.0"
    changes:
      - ClassDiscovery 包扫描；data_source Provider / handler 发现基线
