# 测试用例 — `infra.task_guard`（模块根）

**模块：** `infra.task_guard`  
**覆盖版本：** `0.2.0`

## Scope

验证门面 `TaskGuard` 与 `contracts` / `types`（`test_api.py`）。

| 文件 | 说明 |
|------|------|
| `test_api.py` | 公开 API 契约（`force_run`） |

## 实现测（不纳入公开索引）

| 文件 | 说明 |
|------|------|
| `../core/lease/__test__/test_task_lease.py` | 租约互斥 |
