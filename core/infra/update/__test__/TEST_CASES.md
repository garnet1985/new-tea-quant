# 测试用例 — `infra.update`

**模块：** `infra.update`  
**覆盖版本：** `0.5.0`

## Scope

验证门面 `Update` 与 contracts（对齐 `API.md`）。

| 文件 | 说明 |
|------|------|
| `test_api.py` | 公开 API 契约（`force_run`） |
| `test_post_upgrade_runner.py` | post-upgrade 执行顺序 |

编排相关测试见 `setup/updater/__test__/`。
