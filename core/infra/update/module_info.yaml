name: infra.update
version: 0.5.0
compatible_core_versions: ">=0.4.1"
description: "升级扩展：db 数据迁移脚本注册表（infra.db 调用）；post_upgrade 收尾注册表与 CLI（setup/updater 子进程调用）。编排见 setup/updater。"
dependencies: []
changelog:
  - version: "0.5.0"
    changes:
      - 对齐 CORE_MODULE_STANDARDS：Facade Update（data_scripts / post_upgrade）；contracts；API.md / QUICKSTART / docs / test_api
      - 澄清与 setup/updater 边界；误放本模块的 updater 测试迁至 setup/updater/__test__
      - BREAKING：外部调用方改 Update.*；公开 API 最高 beta
      - 实现下沉 core/；注册表类状态 + clear；Update.types；CLI 改为 -m core.infra.update.core.post_upgrade
  - version: "0.4.1"
    changes:
      - （历史）升级相关能力曾与 setup/updater 交织；现仅保留本模块扩展点
  - version: "0.4.0"
    changes:
      - （历史）注册表基线
  - version: "0.3.1"
    changes:
      - （历史）updater 基础能力预留
