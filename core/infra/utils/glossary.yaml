# glossary — infra.utils
# Version: 0.2.1

terms:
  Facade:
    definition: "门面：对外统一入口类。本模块门面类是 Utils。"
    aliases:
      - "门面"
    avoid:
      - "deep-import core.date / core.io / core.math / core.markdown / core.locale"

  Utils:
    definition: "本模块门面类；挂载 date / types / io / math / markdown / locale。"
    aliases: []
    avoid:
      - "DateUtils 当公开入口"
      - "TypeUtils 当公开入口"

  period_type:
    definition: "日/周/月/季/年周期类型字符串；常量见 contracts.PERIOD_*。"
    aliases:
      - "PERIOD_DAY"
      - "PERIOD_MONTH"
      - "PeriodType"
    avoid: []

  is_china:
    definition: "本机是否在中国大陆；公开入口 Utils.locale.is_china / Utils.is_china。按时区地名 / zh_CN，不用 UTC+8。"
    aliases:
      - "Utils.locale"
      - "LocaleUtils"
    avoid:
      - "按 UTC+8 / CST-8 判断国内"

  MarkdownMgr:
    definition: "Markdown 模版 ``{{:token}}`` 填充器；公开入口 Utils.markdown。"
    aliases:
      - "Utils.markdown"
    avoid:
      - "extract_tokens 当包级导出"

  deterministic_unit_float:
    definition: "由 key 派生的确定性 [0,1) 伪随机；公开入口 Utils.math.deterministic_unit_float。"
    aliases:
      - "Utils.math"
      - "DeterministicRandom"
    avoid: []
