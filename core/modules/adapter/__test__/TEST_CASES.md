# 测试用例 — `modules.adapter`

**模块：** `modules.adapter`  
**覆盖版本：** `0.3.0`

## Scope

验证门面 `Adapter` 与 contracts 公开逻辑（对齐 `API.md`）。

| 文件 | 说明 |
|------|------|
| `test_api.py` | 公开 API 契约（`force_run`） |
