name: modules.indicator
version: 0.3.0
compatible_core_versions: ">=0.4.1"
description: "技术指标计算：Indicator 作为 pandas-ta-classic 的代理，K 线 List[Dict] 与 DataFrame 互转，通用 calculate 与常用便捷方法。"
dependencies: []
changelog:
  - version: "0.3.0"
    changes:
      - "对齐 CORE_MODULE_STANDARDS：Facade Indicator；contracts.BatchIndicatorResult；根 API/QUICKSTART/glossary/TEST_CASES"
      - "BREAKING：包根仅导出 Indicator（原 IndicatorService）；BatchIndicatorResult 从 contracts 导入"
      - "标准化收口（不 bump）：实现下沉 core/；AVAILABLE_INDICATORS→docs/；扩 API/test_api；glossary 合规；_init_ta 短路与 DESIGN monkeypatch 说明"
      - "依赖穿透治理（不 bump）：审计确认无外部 core 依赖；dependencies 保持空；inbound 均走 Facade"
  - version: "0.2.2"
    changes:
      - "（历史误记）changelog 曾写入 K 线 qfq 模拟算法改动；该能力属 data 管线，非本模块。本条仅保留版本占位。"
  - version: "0.2.1"
    changes:
      - K 线指标计算路径优化（与 v0.3.3 core 性能改进对齐）
  - version: "0.2.0"
    changes:
      - IndicatorService、pandas-ta-classic 代理与 AVAILABLE_INDICATORS 注册
  - version: "0.1.0"
    changes:
      - 初始化模块，完成基础功能