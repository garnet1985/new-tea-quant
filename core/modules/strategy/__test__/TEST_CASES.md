# 测试用例 — `modules.strategy`

**模块：** `modules.strategy`  
**覆盖版本：** `0.7.0`

## Scope

验证门面 `Strategy` 与 `contracts` 公开面（对齐 `API.md`）。

引擎 / 服务实现测已下沉至 `core/**/__test__/`，**不在本索引**。

| 文件 | 说明 |
|------|------|
| `test_api.py` | 公开 API 契约与轻量行为（`force_run`） |
