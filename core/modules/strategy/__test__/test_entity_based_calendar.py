#!/usr/bin/env python3
# MARK: STALE — 依赖已 UNUSED 的 EntityBasedExecutePayload / EntityBasedExecutor。
"""entity_based calendar 单元测试（已停用）。"""

from __future__ import annotations

import pytest

pytest.skip(
    "MARK: STALE — EntityBasedExecutor / ExecutePayload archived as UNUSED",
    allow_module_level=True,
)
