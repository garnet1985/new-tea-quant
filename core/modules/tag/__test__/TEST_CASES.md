# 测试用例 — `modules.tag`

**模块：** `modules.tag`  
**覆盖版本：** `0.5.0`

## Scope

验证门面 `Tag` 与 `contracts` 公开面（对齐 `API.md`）。  
实现 / 路由测见 `core/**/__test__/`（含 `core/__test__/test_tag.py`），不在本索引。

| 文件 | 说明 |
|------|------|
| `test_api.py` | 公开 API 契约与轻量行为（`force_run`） |
