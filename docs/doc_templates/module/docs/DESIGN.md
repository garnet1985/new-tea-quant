# <Module Display Name> — 设计

<!--
  可选：无重要选型时删除本文件。
  按时间追加「设计点 N」，不覆盖历史结论。
-->

**版本：** `<module.version>`

`<一两句：本文覆盖哪些设计点。>`  
相关：[ARCHITECTURE.md](./ARCHITECTURE.md)

---

## 设计点 1：`<短标题>`

### 设计初衷

`<解决什么问题？>`

### 设计背景

`<约束、上下文、为何此时要设计。>`

### 解决方案

| 方案 | 做法摘要 | 优点 | 缺点 |
|------|----------|------|------|
| A：`<名>` | `<…>` | `<…>` | `<…>` |
| B：`<名>` | `<…>` | `<…>` | `<…>` |

### 决定

- **采用：** `<方案 X>`
- **理由：** `<为什么选它>`
- **影响：** `<可选>`

---

## 设计点 2：`<短标题>`

### 设计初衷

`<解决什么问题？>`

### 设计背景

`<…>`

### 解决方案

| 方案 | 做法摘要 | 优点 | 缺点 |
|------|----------|------|------|
| A：`<名>` | `<…>` | `<…>` | `<…>` |
| B：`<名>` | `<…>` | `<…>` | `<…>` |

### 决定

- **采用：** `<方案 X>`
- **理由：** `<…>`
- **影响：** `<…>`

---

## 相关文档

- [架构](./ARCHITECTURE.md)
- [API.md](../API.md)
- [术语表](../glossary.yaml)
