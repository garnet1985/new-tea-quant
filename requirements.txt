# New Tea Quant — 运行主程序所需依赖（Python 3.9+）
# 开发与测试：requirements-dev.txt
# 部分 RTB/ML 脚本绘图需 matplotlib：按需 pip install matplotlib

# Database（适配器工厂会同时加载 MySQL / PostgreSQL 模块）
pymysql>=1.1.0
psycopg2-binary>=2.9.0

# Data
pandas>=2.0.0
numpy>=1.24.0

# 技术指标（indicator_service）
pandas-ta-classic>=0.3.59

# HTTP / 行情数据源
requests>=2.28.0
curl_cffi>=0.5.0
tushare>=1.2.0
akshare>=1.17.0

# Worker / 资源监控
psutil>=5.9.0
